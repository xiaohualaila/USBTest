package com.example.administrator.greendaodemo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.administrator.bean.WhiteList;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WHITE_LIST".
*/
public class WhiteListDao extends AbstractDao<WhiteList, Long> {

    public static final String TABLENAME = "WHITE_LIST";

    /**
     * Properties of entity WhiteList.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Num = new Property(1, String.class, "num", false, "NUM");
        public final static Property Name = new Property(2, String.class, "name", false, "NAME");
        public final static Property CardCode = new Property(3, String.class, "cardCode", false, "CARD_CODE");
        public final static Property IdCardNo = new Property(4, String.class, "idCardNo", false, "ID_CARD_NO");
        public final static Property Company = new Property(5, String.class, "company", false, "COMPANY");
        public final static Property Work = new Property(6, String.class, "work", false, "WORK");
        public final static Property Certificates = new Property(7, String.class, "certificates", false, "CERTIFICATES");
    }


    public WhiteListDao(DaoConfig config) {
        super(config);
    }
    
    public WhiteListDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WHITE_LIST\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"NUM\" TEXT," + // 1: num
                "\"NAME\" TEXT," + // 2: name
                "\"CARD_CODE\" TEXT," + // 3: cardCode
                "\"ID_CARD_NO\" TEXT," + // 4: idCardNo
                "\"COMPANY\" TEXT," + // 5: company
                "\"WORK\" TEXT," + // 6: work
                "\"CERTIFICATES\" TEXT);"); // 7: certificates
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WHITE_LIST\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WhiteList entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String num = entity.getNum();
        if (num != null) {
            stmt.bindString(2, num);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        String cardCode = entity.getCardCode();
        if (cardCode != null) {
            stmt.bindString(4, cardCode);
        }
 
        String idCardNo = entity.getIdCardNo();
        if (idCardNo != null) {
            stmt.bindString(5, idCardNo);
        }
 
        String company = entity.getCompany();
        if (company != null) {
            stmt.bindString(6, company);
        }
 
        String work = entity.getWork();
        if (work != null) {
            stmt.bindString(7, work);
        }
 
        String certificates = entity.getCertificates();
        if (certificates != null) {
            stmt.bindString(8, certificates);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WhiteList entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String num = entity.getNum();
        if (num != null) {
            stmt.bindString(2, num);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        String cardCode = entity.getCardCode();
        if (cardCode != null) {
            stmt.bindString(4, cardCode);
        }
 
        String idCardNo = entity.getIdCardNo();
        if (idCardNo != null) {
            stmt.bindString(5, idCardNo);
        }
 
        String company = entity.getCompany();
        if (company != null) {
            stmt.bindString(6, company);
        }
 
        String work = entity.getWork();
        if (work != null) {
            stmt.bindString(7, work);
        }
 
        String certificates = entity.getCertificates();
        if (certificates != null) {
            stmt.bindString(8, certificates);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public WhiteList readEntity(Cursor cursor, int offset) {
        WhiteList entity = new WhiteList( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // num
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // name
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // cardCode
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // idCardNo
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // company
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // work
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // certificates
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WhiteList entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNum(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCardCode(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setIdCardNo(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setCompany(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setWork(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setCertificates(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(WhiteList entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(WhiteList entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(WhiteList entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
